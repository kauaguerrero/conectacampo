// Quantas mensagens com feedback (positivo/médio/negativo) precisam existir
// antes do modo "robusto" (busca semântica) ficar disponível. Só existe o
// switch aqui — o motor de busca semântica em si ainda não foi implementado.
export const KNOWLEDGE_BASE_THRESHOLD = 30;
